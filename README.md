# express-prisma

## Project Setup

1. Install Dependencies

    ```sh
    npm install
    ```

2. Run Prisma migrations

    ```sh
    npx prisma migrate dev
    ```

3. Run Prisma seeds

    ```sh
    npx prisma db seed
    ```
